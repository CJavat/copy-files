{
  "name": "copy-files",
  "version": "1.0.0",
  "description": "Application to copy files in another directory.",
  "main": "index.ts",
  "scripts": {
    "start": "npx tsc && node dist/index.js"
  },
  "devDependencies": {
    "@types/node": "^22.7.9",
    "rimraf": "^6.0.1",
    "ts-node-dev": "^2.0.0",
    "typescript": "^5.6.3"
  },
  "dependencies": {
    "inquirer": "^12.0.0"
  }
}
